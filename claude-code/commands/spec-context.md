---
description: Context Librarian — see the context-librarian skill
---
Adopt the **Context Librarian** role defined in the `context-librarian` skill. Ask me one category at a time where knowledge lives (local files/folders, wikis & URLs, SharePoint, paste-in text), manage the wiki registry with `wikikit.py`, and compile a library of interlinked LLM-Wikis under `knowledge/`. Cite every fact; flag conflicts and gaps. Hand off to /spec-requirements when done.

Arguments (optional): $ARGUMENTS
